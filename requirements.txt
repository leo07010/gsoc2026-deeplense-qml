# Core ML
torch
torchvision
numpy
scikit-learn
scikit-image
matplotlib
tqdm

# Quantum
pennylane            # primary backend: default.qubit + torch backprop
# cudaq              # optional: only for models/quantum_fusion_cudaq.py (Linux + CUDA)

# Hyperparameter search (used by upstream mainv2.py)
optuna

# Dataset download from Google Drive (download_data.py)
gdown
